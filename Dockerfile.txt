# Estágio 1: Build da Aplicação React
FROM node:18-slim as build

WORKDIR /app

# Copia arquivos de configuração
COPY package*.json ./

# Instala dependências com flags para evitar erros de compatibilidade e peer deps
# O --loglevel verbose ajuda a ver o erro real se falhar novamente
RUN npm install --legacy-peer-deps --loglevel verbose

# Copia todo o código fonte
COPY . .

# Gera o build de produção
RUN npm run build

# Estágio 2: Servidor Web Nginx
FROM nginx:alpine

# Remove configurações padrão do Nginx
RUN rm -rf /etc/nginx/conf.d/*

# Copia o build gerado no estágio anterior
COPY --from=build /app/dist /usr/share/nginx/html

# Copia a configuração customizada do Nginx
COPY nginx.conf.txt /etc/nginx/conf.d/default.conf

# Expõe a porta 80
EXPOSE 80

# Inicia o Nginx
CMD ["nginx", "-g", "daemon off;"]